**Full checklist:** `.github/skills/security-audit/SKILL.md`API keys never in source/config/logs — `CredentialRef` = env var **name** only`os.Getenv()` only in `backend/internal/platform/config/config.go` and `agent/internal/config/config.go``llm_config` JSONB: `{provider, model, credential_ref}` onlyMissing credential at startup → agent unavailable (no silent fallback)HTTP handlers validate input (UUID, bounds) → 400 on violationParameterized SQL only — no string interpolationNever log resolved credential values