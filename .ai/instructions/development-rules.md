Design before code — `brainstorming` skillVertical slice per module — `.github/skills/vertical-slice/SKILL.md`No cross-module internal imports — `.github/skills/modularity/SKILL.md`DB access via module `repository.go` onlyLLM via `LLMProvider` interface only — `platform/llm/`Config via env vars — getters in `config.go` onlyStructured logging — `log/slog`; no `fmt.Println`Tests without network — mocks/fakesOne canonical location per concept